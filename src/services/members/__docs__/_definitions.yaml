definitions:
  Member:
    type: 'object'
    properties:
      id:
        type: 'integer'
        description: 'Unique id'
      isOwner:
        type: 'boolean'
      lastVisitedChannelId:
        type: 'integer'
      userId:
        type: 'integer'
      guildId:
        type: 'integer'
      createdAt:
        type: 'string'
        format: 'date-time'
      updatedAt:
        type: 'string'
        format: 'date-time'
