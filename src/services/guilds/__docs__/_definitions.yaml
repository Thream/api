definitions:
  Guild:
    type: 'object'
    properties:
      id:
        type: 'integer'
        description: 'Unique id'
      name:
        type: 'string'
        description: 'Unique name'
      description:
        type: 'string'
      icon:
        type: 'string'
      isPublic:
        type: 'boolean'
      createdAt:
        type: 'string'
        format: 'date-time'
      updatedAt:
        type: 'string'
        format: 'date-time'
